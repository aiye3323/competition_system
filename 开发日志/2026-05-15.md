# 2026-05-15 开发日志

## 今天完成

### 1. Impeccable 前端重设计（全局）
- 建立 PRODUCT.md 产品设计策略文档：确定 product register，专业严谨/学术可信/清晰内敛
- 建立 DESIGN.md 视觉设计系统文档：暖灰底 + 靛蓝点缀配色方案
- 参考：飞书/Lark 清爽组织感
- 主色调：`#4C51BF`（靛蓝），替代原 `#1883E5`（SaaS 蓝）

### 2. 全局 CSS 重写（frontend/src/assets/styles/global.css）
- 全套 CSS 变量重定义：靛蓝主色、暖灰背景、语义色、阴影系统
- Element Plus 全局覆盖：卡片/表格/按钮/标签/输入框/分页/弹窗
- 去掉卡片彩色顶边，改用圆角 + 微阴影
- 去掉表格纵向分割线，保留横向浅线
- 标签改为 pill 形状 + 浅底深字
- 输入框聚焦态：靛蓝边框 + 淡蓝阴影

### 3. 布局重设计（AppLayout.vue）
- 去掉 emoji logo，改用 SVG 图标
- 导航项用 router-link + 底线指示活跃态
- 用户区改为靛蓝圆形头像（首字母）
- 下拉菜单增加用户信息和角色显示

### 4. 登录/注册页面重设计
- 深靛蓝渐变背景 + 半透明装饰圆形
- 白色卡片 + SVG logo + 加粗标题
- 统一 12px 圆角 + 24px 大阴影
- 注册页改为标签式表单（无 label-width）

### 5. Dashboard 仪表盘重设计
- 统计卡片改为图标 + 数值并排布局（去掉 colored top border 模式）
- 成果总计卡片用靛蓝实底突出
- ECharts 配色统一为靛蓝/翠绿/琥珀/紫
- 柱状图加圆角顶边

### 6. 列表页统一样式更新
- 竞赛/项目/论文/软著 4 个列表页统一模板
- 筛选区用 card-wrapper 包裹
- 表格统一去掉 border 属性
- 操作按钮组用 header-actions 样式

### 7. 审核管理/通知/后台页面更新
- AuditList/SecretaryAudit/LeaderAudit 去掉表格 border
- review/List 去掉表格 border
- admin/UserManagement/OperationLogs 去掉表格 border
- achievement/PublicList 去掉表格 border

### 8. Git 推送
- 添加远程仓库 origin → https://github.com/aiye3323/competition_system
- 推送 master 分支成功

## 项目新增文件
- `/PRODUCT.md` — 产品设计策略
- `/DESIGN.md` — 视觉设计系统
- `.agents/skills/impeccable/` — Impeccable 设计技能

## 明天待办
- 根据实际使用反馈微调配色和间距
- 考虑添加暗色模式支持
- 第7阶段部署（7.7）

### 9. 修复文件预览和下载报错
- 报错原因：`Paths.get(uploadPath, storagePath)` 每次都重新拼接，中文路径下拼接结果不稳定
- 修复方案：改为启动时用 `Paths.get(uploadPath).toAbsolutePath().normalize()` 缓存 `baseUploadPath`，后续用 `baseUploadPath.resolve(storagePath)`
- FileController 增加 try-catch：文件不存在返回 400 JSON 错误，记录不存在返回 404
- 增加路径遍历安全检查：`filePath.startsWith(baseUploadPath)`
- 测试结果：上传 → 预览 HTTP 200 ✅，下载 HTTP 200 ✅，不存在文件返回 404 JSON ✅

### 10. 全面优化文件预览和下载功能

**10.1 多格式预览支持（FullscreenPreview.vue）**
- 新增 DOC/DOCX/XLS/PPT 预览：通过微软 Office Online iframe 嵌入
- 新增 TXT 文本预览：fetch 读取内容 + 代码编辑器风格展示
- 新增 MP4/AVI/MOV 视频预览：HTML5 video 标签
- ZIP 压缩包提示"无法预览，请下载后查看"

**10.2 下载文件命名规范**
- 后端新增 `buildDownloadName()` 方法，生成格式：`【类型】_【申报人】_【名称】_【用途】.ext`
- 下载响应头使用 `filename*=UTF-8''` RFC 5987 编码，解决中文乱码

**10.3 ZIP 打包下载（后端）**
- `FileStorageService.zipFiles()`：内存中 ZIP 打包，同名文件自动去重
- `GET /api/files/download-all/{type}/{id}`：下载单个成果全部材料
- `POST /api/files/download-selected`：下载勾选文件

**10.4 前端附件显示增强（AttachmentDisplay.vue）**
- 新增文件复选框 + 全选/取消全选
- 新增"下载选中 (N)"按钮 + "下载全部材料"按钮
- 文件图标按类型区分（文档/视频）

**10.5 详情页面接入**
- competition/project/paper/software 四个 Detail.vue 传入 `relatedType` + `relatedId`

**修改文件**：FileStorageService.java，FileController.java，file.js，FullscreenPreview.vue，AttachmentDisplay.vue，4x Detail.vue

**测试结果**：
- download-selected [27,31] → HTTP 200，282 字节 ZIP ✅
- download-all COMPETITION/999（无文件）→ 404 + JSON 提示 ✅
- 后端启动正常，前端无编译错误

### 11. 修复批量下载 ZIP 命名 + DOC/DOCX 预览

**11.1 批量下载 ZIP 命名规范**
- `downloadSelected` 接口改为接收 `typeName`/`applicantName`/`achievementName` 参数
- 单成果 ZIP 名：`项目_张三_预览测试_选中材料.zip`
- 跨成果 ZIP 名：`批量成果_操作人_20260515.zip`
- 前端 AttachmentDisplay 新增 `achievementName`/`applicantName` props
- 四个 Detail.vue 统一传入成果名称和申报人

**11.2 DOC/DOCX 预览修复**
- Office Online 无法访问 localhost 内网文件
- FullscreenPreview 改为直接显示"Office 文档暂不支持在线预览，请下载后查看"

**11.3 批量下载 loading 提示**
- 超过 10 个文件时，ElMessage.info 显示"正在打包文件，请稍候…（共 N 个文件）"

**测试结果**：
- download-selected 含 metadata → HTTP 200，282 字节 ZIP ✅
- 后端启动正常

### 12. 文件命名规范 + 预览组件

**12.1 文件命名工具重写（fileNaming.js）**
- 按四种成果类型精准命名：学科竞赛/创新项目/学术论文/软件著作
- 多人取第一 + "等"（`getFirstPerson()`）
- 材料类型自动推断（`inferMaterialType()`）
- 新增 `downloadBlob()` blob 下载函数

**12.2 新建组件**
- `AttachmentItem.vue`：单文件卡片组件，显示规范名+原始名、已下载标记、预览/下载按钮
- `FilePreviewModal.vue`：el-dialog 预览弹窗，支持 DOCX(mammoth)/图片/PDF/TXT/视频

**12.3 命名规范文档**
- 创建 `/命名规范.md` — 完整命名规则、特殊规则、代码位置

**12.4 集成**
- AttachmentDisplay 和 FullscreenPreview 中的 downloadFile/downloadCurrent 已使用新命名
- 文件列表显示规范名称

### 13. 重构附件组件 + 修复 inline 预览编码

**13.1 删除"下载全部材料"按钮**
- AttachmentDisplay 移除"下载全部材料"按钮，只保留"全选"和"下载选中(N)"

**13.2 文件名简化**
- `displayName()` 改为只显示 `材料类型.扩展名`（如"获奖证书.png"）
- `getMaterialLabel()` 从原始文件名推断材料类型
- 鼠标悬停 tooltip 显示原始文件名

**13.3 后端 inline 预览修复**
- FileController inline 端点增加 `filename*=UTF-8''` 编码
- 修复中文文件名被 Tomcat 剥离 Content-Disposition 头的问题

**13.4 AuditList 页面接入**
- AttachmentDisplay 传入 `relatedType`、`achievementName`、`applicantName`

### 14. 修复上传文件名显示 + 下载命名优化

**14.1 AttachmentDisplay 新增 materialMap prop**
- 支持 `{ fileId: "获奖证书" }` 精确映射，优先于关键词推断
- `getMaterialLabel()` 改进：增加更多中文关键词，无法推断时取原始文件名前10字

**14.2 FileUpload 上传时按区域重命名**
- `handleSuccess` 中按 `materialType` 重命名文件显示名
- 例如上传到"获奖证书"区域 → 文件列表显示"获奖证书.docx"

**14.3 下载名简化**
- 下载使用 `displayName()`（材料类型.扩展名），如"获奖证书.png"
- 移除 `generateFileName` 依赖，代码更简洁

**修改文件**：AttachmentDisplay.vue，FileUpload.vue

### 15. 创建上传区域配置模块

- 新建 `utils/uploadConfig.js`：四种成果类型 11 个上传区域配置
- FileUpload 升级支持 `areaConfig` prop（maxCount/accept/maxSize 配置驱动）
- submit/Index.vue 11 处 FileUpload 全部改为配置驱动

**修改文件**：uploadConfig.js，FileUpload.vue，submit/Index.vue

### 16. 修复 Word 预览背景 + 图片预览健壮性

- WordPreviewModal：背景从深色 `#1e1e2e` 改为白色 `#ffffff`，文字色同步调整
- AttachmentDisplay.canPreview：增加图片扩展名 fallback 检测

**修改文件**：WordPreviewModal.vue，AttachmentDisplay.vue

## 问题
### 17. 修复下载命名关联成果信息

**问题**：单文件下载名只显示 `材料类型.扩展名`，缺少成果和申报人信息

**修复**：
- AttachmentDisplay：恢复 `getNamingParams()`，`downloadFile` 改用 `generateFileName()` 生成完整规范名如 `竞赛_张三_全国大学生数学竞赛_获奖证书.png`
- FullscreenPreview：新增 `namingParams` prop，`downloadCurrent` 传入命名上下文
- AttachmentDisplay 传 `:naming-params="getNamingParams()"` 给 FullscreenPreview
- FileController.downloadFile：无 filename 参数时用 `materialType + ext` 兜底
- 批量 ZIP 命名用 `generateBatchZipName()`

**修改文件**：AttachmentDisplay.vue，FullscreenPreview.vue，FileController.java

### 18. 修正命名格式 + 统一预览

- fileNaming.js：`join('-')`→`join('_')`，TYPE_LABEL 短化（竞赛/项目/论文/软著）
- FullscreenPreview：Word 文档用 Office Online iframe，localhost 回退 mammoth.js
- 图片预览：canPreview 已有扩展名 fallback（第15项已修复）

**修改文件**：fileNaming.js，FullscreenPreview.vue

## 问题
- 无
